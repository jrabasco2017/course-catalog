<%= form_for(course) do |f| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <%course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.time_select :name %>
  </div>

  <div class="field">
    <%= f.label :idep %>
    <%= f.time_select :idep %>
  </div>

  <div class="field">
    <%= f.label :subjects %>
    <%= f.time_select :subjects %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
