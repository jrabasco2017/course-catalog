<%= form_for(subject) do |f| %>
  <% if subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subject.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <%subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.time_select :name %>
  </div>

  <div class="field">
    <%= f.label :ider %>
    <%= f.time_select :ider %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
