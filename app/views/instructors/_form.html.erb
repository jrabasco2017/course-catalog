<%= form_for(instructor) do |f| %>
  <% if instructor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instructor.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <%instructor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first %>
    <%= f.time_select :first %>
  </div>

  <div class="field">
    <%= f.label :last%>
    <%= f.time_select :last %>
  </div>

  <div class="field">
    <%= f.label :ident%>
    <%= f.time_select :ident %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
